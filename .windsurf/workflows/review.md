1. Debug TypeScript/React/Node Errors
Analise erros de TypeScript, React, React Native, Node.js e Express. Encontre a causa raiz, explique o impacto e aplique a menor correção possível sem refatorar partes não relacionadas.

2. Backend API Review
Revise rotas, controllers, services e middlewares do backend. Verifique segurança, JWT, validação, tratamento de erro, status codes, variáveis de ambiente e padrão REST /api/v1.

3. Frontend UI/UX Implementation
Implemente telas React/React Native com foco em responsividade, organização visual, componentes reutilizáveis e sem alterar regras de negócio ou backend.

4. Database & PostgreSQL Review
Revise modelagem, queries, migrations, relações, índices e consistência dos dados no PostgreSQL. Evite queries inseguras ou lentas.

5. Environment Variables & Security Check
Procure secrets hardcoded, JWT_SECRET fraco, DATABASE_URL exposta, tokens no frontend, .env ausente do .gitignore e variáveis obrigatórias faltando no deploy.

6. Railway Deploy Fix
Diagnostique erros de deploy no Railway, build, start command, variáveis de ambiente, porta dinâmica process.env.PORT e geração da pasta dist.

7. Mobile App Crash Debug
Investigue crashes em React Native/Expo, permissões Android, build APK/AAB, problemas de ambiente, logs e diferenças entre dev, APK e produção.

8. AI Integration Workflow
Implemente integração com OpenAI/Whisper/OCR usando backend seguro, controle de tokens, validação de entrada, tratamento de erro e sem expor API keys no frontend.

9. PR Generator
Gere título, descrição, resumo técnico, mudanças feitas, testes realizados e riscos do Pull Request com base nas alterações atuais.

10. Refactor Safely
Refatore código apenas quando necessário, mantendo comportamento existente, sem quebrar rotas, tipos, contratos da API ou layout atual.
